meta {
  name: Get User 1
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/users/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{user1Token}}
}

tests {
  test("User 1 is returned", function () {
    expect(res.status).to.equal(200);
    expect(res.body.id).to.equal(bru.getEnvVar("user1Id"));
  });
}
