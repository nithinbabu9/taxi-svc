meta {
  name: User 1 Accepts Match
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/matches/{{matchId}}/accept
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "userId": "{{user1Id}}"
  }
}

tests {
  test("First acceptance succeeds", function () {
    expect(res.status).to.equal(200);
  });
}
