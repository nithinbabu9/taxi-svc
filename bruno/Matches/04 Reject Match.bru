meta {
  name: Reject Match (Alternative Flow)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/matches/{{matchId}}/reject
  body: none
  auth: bearer
}

auth:bearer {
  token: {{user1Token}}
}
