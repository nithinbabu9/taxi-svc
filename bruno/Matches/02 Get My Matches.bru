meta {
  name: Get My Matches
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/matches/mine
  body: none
  auth: bearer
}

auth:bearer {
  token: {{user1Token}}
}

tests {
  test("My matches are returned", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
