meta {
  name: Get User 1 Rides
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/users/{{user1Id}}/rides
  body: none
  auth: none
}

tests {
  test("Ride list is returned", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
