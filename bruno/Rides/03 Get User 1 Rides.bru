meta {
  name: Get User 1 Rides
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/rides/mine
  body: none
  auth: bearer
}

auth:bearer {
  token: {{user1Token}}
}

tests {
  test("Ride list is returned", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
