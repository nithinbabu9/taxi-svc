meta {
  name: Cancel Ride 1
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/rides/{{ride1Id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{user1Token}}
}

tests {
  test("Ride is cancelled", function () {
    expect(res.status).to.equal(200);
    expect(res.body.status).to.equal("CANCELLED");
  });
}
