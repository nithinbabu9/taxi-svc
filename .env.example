# Copy these values into your IDE run configuration or shell environment.
# Never commit real credentials or API keys.
DATABASE_URL=jdbc:postgresql://localhost:5432/taxi
DATABASE_USERNAME=taxi_app
DATABASE_PASSWORD=replace-with-your-local-password
GOOGLE_MAPS_API_KEY=replace-with-your-restricted-server-key
